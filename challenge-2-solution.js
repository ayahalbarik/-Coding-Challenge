'use strict';

var number=prompt("1,2,7");

 let array=[];
function max (){
  for (var i=0;i <array.length;i++){
if (array[i]>0){

  array.prototype.max=function () {
    return Math.max.apply(this.arrays);

  
}else {


  array.prototype.max=function (){
    return Math.min.apply(this.arrays);

    
    console.log (min);

}
}